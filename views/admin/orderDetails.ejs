<!DOCTYPE html>
<html>
<head>
    <title>Order Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background: #0f0f0f;
            color: #eee;
            font-family: 'Poppins', sans-serif;
        }

        .neon-card {
            margin-top: 40px;
            border-radius: 18px;
            padding: 25px;
            background: rgba(20,20,20,0.85);
            border: 2px solid #ff0066;
            box-shadow: 0 0 25px #ff0066aa;
        }

        .product-img {
            width: 75px;
            height: 75px;
            border-radius: 12px;
            object-fit: contain;
            background: #fff;
            padding: 6px;
        }

        .title {
            margin-top: 25px;
            text-align: center;
            font-size: 2rem;
            color: #ff0066;
            font-weight: 700;
            text-shadow: 0 0 15px #ff0066aa;
        }

        .btn-back {
            background: #ff0066;
            color: #fff;
            font-weight: bold;
            border-radius: 14px;
            padding: 10px 22px;
        }
        .btn-back:hover {
            background: #ff4d88;
        }
    </style>
</head>
<body>

<%- include('../partials/navbar', { user: user }) %>

<h2 class="title">Order #<%= orderId %> Details</h2>

<div class="container">
    <div class="neon-card">

        <table class="table table-dark table-borderless align-middle">
            <thead>
                <tr>
                    <th>Image</th>
                    <th>Item</th>
                    <th>Qty</th>
                    <th>Unit Price</th>
                </tr>
            </thead>

            <tbody>
                <% items.forEach(item => { %>
                <tr>
                    <td><img src="/images/<%= item.image %>" class="product-img"></td>

                    <td><%= item.productName %></td>
                    <td><%= item.quantity %></td>
                    <td>$<%= item.unit_price.toFixed(2) %></td>
                </tr>
                <% }) %>
            </tbody>

        </table>

        <a href="/admin/orders" class="btn-back mt-3">← Back to Orders</a>

    </div>
</div>

</body>
</html>
