<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmed - FairPrice App</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background: #fafafa;
            font-family: 'Poppins', sans-serif;
        }

        .confirmation-card {
            background: rgba(255,255,255,0.65);
            padding: 40px;
            border-radius: 20px;
            margin-top: 40px;
            backdrop-filter: blur(8px);
            box-shadow: 0 8px 20px rgba(0,0,0,0.1);
            text-align: center;
        }

        .success-icon {
            font-size: 5rem;
            color: #28a745;
            margin-bottom: 20px;
        }

        .price-tag {
            color: #ff3d3d;
            font-weight: bold;
            font-size: 1.3rem;
        }

        .btn-continue {
            background: #ff4d4d;
            border: none;
            padding: 12px 20px;
            border-radius: 20px;
            font-size: 1.2rem;
            font-weight: bold;
            color: white;
            margin-top: 20px;
            width: 100%;
        }

        .btn-continue:hover {
            background: #e60000;
        }
    </style>
</head>

<body>

<%- include('../partials/navbar', { user: user }) %>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-8">

            <div class="confirmation-card">

                <div class="success-icon">✔</div>

                <h2 class="fw-bold">Order Confirmed!</h2>
                <p class="text-muted mb-4">
                    Thank you for your purchase. Your order number is <strong>#<%= order.id %></strong>.
                </p>

                <h4 class="fw-bold mt-4 text-start">Order Summary</h4>

                <table class="table mt-2 align-middle">
                    <thead>
                        <tr>
                            <th>Item</th>
                            <th>Qty</th>
                            <th>Total</th>
                        </tr>
                    </thead>

                    <tbody>
                        <% order.items.forEach(item => { %>
                            <tr>
                                <td><%= item.productName %></td>
                                <td><%= item.quantity %></td>
                                <td class="price-tag">
                                    $<%= (item.quantity * item.unit_price).toFixed(2) %>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>

                <h4 class="text-end fw-bold mt-3">
                    Grand Total:
                    <span class="price-tag">$<%= order.total.toFixed(2) %></span>
                </h4>

                <a href="/shopping">
                    <button class="btn-continue">Continue Shopping</button>
                </a>

            </div>

        </div>
    </div>
</div>

</body>
</html>
