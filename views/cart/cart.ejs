<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Cart - FairPrice App</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background: #fafafa;
            font-family: 'Poppins', sans-serif;
        }

        .page-title {
            font-weight: 700;
            text-align: center;
            margin-top: 30px;
            margin-bottom: 20px;
            color: #444;
        }

        .cart-card {
            background: rgba(255,255,255,0.6);
            border-radius: 20px;
            backdrop-filter: blur(5px);
            padding: 20px;
            box-shadow: 0 6px 16px rgba(0,0,0,0.1);
        }

        .product-img {
            width: 80px;
            height: 80px;
            object-fit: contain;
            border-radius: 10px;
        }

        .btn-update {
            background: #ff4d4d;
            color: white;
            border-radius: 15px;
            padding: 6px 14px;
            border: none;
        }

        .btn-remove {
            border-radius: 15px;
            padding: 6px 14px;
        }

        .checkout-btn {
            background: #ff4d4d;
            border: none;
            padding: 12px;
            border-radius: 20px;
            font-size: 1.2rem;
            font-weight: bold;
            width: 100%;
            color: white;
            box-shadow: 0 4px 10px rgba(0,0,0,0.15);
        }
    </style>
</head>

<body>

<%- include('../partials/navbar', { user: user }) %>

<div class="container mt-3">
    <%- include('../partials/alerts', { errors: errors, messages: messages }) %>
</div>

<h2 class="page-title"> Your Shopping Cart</h2>

<div class="container">
    <div class="cart-card">

        <% if (cart.length === 0) { %>
            <p class="text-center fw-bold mt-3">Your cart is empty.</p>
        <% } else { %>

            <table class="table align-middle">
                <thead>
                    <tr>
                        <th>Item</th>
                        <th>Qty</th>
                        <th>Price</th>
                        <th>Total</th>
                        <th></th>
                    </tr>
                </thead>

                <tbody>

                <% let grandTotal = 0; %>

                <% cart.forEach(item => { %>

                    <% let total = item.price * item.quantity; %>
                    <% grandTotal += total; %>

                    <tr>
                        <td>
                            <div class="d-flex align-items-center">
                                <img src="/images/<%= item.image %>" class="product-img me-3">
                                <strong><%= item.productName %></strong>
                            </div>
                        </td>

                        <td>
                            <form action="/cart/update" method="POST" class="d-flex align-items-center">
                                <input type="hidden" name="id" value="<%= item.id %>">
                                <input type="number" name="quantity" min="1" value="<%= item.quantity %>" class="form-control" style="width: 70px; border-radius: 12px;">
                                <button class="btn-update ms-2">Update</button>
                            </form>
                        </td>

                        <td>$<%= item.price.toFixed(2) %></td>
                        <td>$<%= total.toFixed(2) %></td>

                        <td>
                            <a href="/cart/remove/<%= item.id %>" class="btn btn-outline-danger btn-remove">Remove</a>
                        </td>
                    </tr>

                <% }) %>

                </tbody>
            </table>

            <h4 class="text-end fw-bold mt-3">Grand Total: $<%= grandTotal.toFixed(2) %></h4>

            <a href="/checkout">
                <button class="checkout-btn mt-3">Proceed to Checkout</button>
            </a>

        <% } %>

    </div>
</div>

</body>
</html>
